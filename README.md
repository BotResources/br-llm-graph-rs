# br-llm-graph

LangGraph-like orchestration over br-llm-messages: a typed key/value state, async nodes, sync edges, supersteps with fan-out and join, a react loop. Not on crates.io; pin the git tag:

```toml
br-llm-graph = { git = "https://github.com/BotResources/br-llm-graph-rs", package = "br-llm-graph", tag = "vX.Y.Z", version = "X.Y.Z" }
```

No support is provided; paid support is available on request from BotResources. Apache-2.0. Generated by an AI system (Anthropic Claude) under the direction and review of BotResources, which takes full responsibility for it (EU AI Act, Regulation (EU) 2024/1689).
